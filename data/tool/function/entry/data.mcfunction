#runs another function with everything in an entry
#with {index, command}
#>mojang collects all data inside for read only (macros)


#>run function
$return run $(command) with storage tool:select list[$(index)]
